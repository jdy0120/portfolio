import styled from "@emotion/styled";
import { motion } from "motion/react";

const LinkStyles = styled(motion.div)`
  position: relative;
  display: flex;
  align-items: center;
  gap: 10px;
`;

export { LinkStyles };
